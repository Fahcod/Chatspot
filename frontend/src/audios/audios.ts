//this file contains audio files tha are being used in the project
import message_sent from "./my_message_sent.wav";
import new_message from "./new_message.wav"
import new_notification from "./new_notification.wav";
import typing_effect from "./typing.mp3";

export const audios={
    message_sent,
    new_message,
    new_notification,
    typing_effect
}